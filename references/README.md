# Reference taxonomy and workbook seeds for container bootstrap (see runtime_config.py).
# Copy Taxonomy.csv and CS_ticket_new_categorizations.xlsx here for image builds
# that omit doc/; Drive sync remains the deployed source of truth.
